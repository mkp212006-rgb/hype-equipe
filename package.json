{
  "name": "hype-equipe",
  "version": "2.3.0",
  "private": true,
  "type": "module",
  "description": "Backend do Tw Store com contas, carteira BRL, Mercado Pago, categorias, serviços SMM e acessos VPN automáticos.",
  "main": "src/launcher.js",
  "scripts": {
    "start": "node src/launcher.js",
    "check": "node --check src/launcher.js && node --check src/server.js && node --check src/app.js && node --check src/db.js && node --check src/support-features.js && node --check src/vpn-features.js && node --check src/smm-client.js && node --check src/mercado-pago-client.js && node --check public/features-v3.js && node --check public/vpn-v1.js"
  },
  "engines": {
    "node": ">=22 <25"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "pg": "^8.16.3"
  }
}

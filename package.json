{
  "name": "hype-equipe-backend",
  "version": "2.1.0",
  "private": true,
  "description": "Backend do Tw Store com contas, carteira BRL, Mercado Pago e precificacao por 1.000.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/db.js && node --check src/provider.js && node --check src/mercadopago.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3"
  }
}
